# Discord.js Slotbot Sniper
A tool in Discord.js used to automatically snipe slotbot.

# Requirements
- Node & NPM
  - `npm install chalk | npm install discord.js-selfbot | npm install node-bash-title`
  - `node index.js` or `node .`
